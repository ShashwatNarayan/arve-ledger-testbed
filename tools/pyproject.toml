[tool.poetry]
name = "ledger-tools"
version = "0.1.0"
description = "Developer tooling for the ledger monorepo"
authors = ["Acme Ledger <dev@acme-ledger.example>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
wheel = "0.37.0"
